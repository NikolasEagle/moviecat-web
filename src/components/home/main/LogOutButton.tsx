import React from "react";

import styles from "./LogOutButton.module.scss";

const LogOutButton = () => {
  return <button className={styles.logout_button}></button>;
};

export default LogOutButton;
