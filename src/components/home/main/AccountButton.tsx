import React from "react";

import styles from "./AccountButton.module.scss";

const AccountButton = () => {
  return <button className={styles.account_button}></button>;
};

export default AccountButton;
